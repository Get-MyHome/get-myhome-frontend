/**
 * 기획서 13절 에러 정의(E). 문구는 확정값이라 그대로 쓴다.
 * 각 에러는 "원인 1줄 + 다음 행동 1줄" 형식이며, 다음 행동 버튼 없는 에러 화면은 금지.
 */

/** E-011 공고문 미분석 단지에서 판정 시도. {count} = 준비 완료 단지 수 */
export const E011_NOTICE_NOT_READY = (count: number) =>
  `이 단지는 판정 준비 중이에요. 준비 완료 단지: ${count}개`;

/** E-012 공고 로딩 실패 */
export const E012_NOTICE_LOAD_FAILED =
  "청약 정보를 불러오지 못했어요. 잠시 후 다시 확인해주세요";

/** 기획서에 번호는 없지만 404(존재하지 않는 공고)용. 원인+행동 형식 유지 */
export const NOTICE_NOT_FOUND = "해당 공고를 찾을 수 없습니다";
